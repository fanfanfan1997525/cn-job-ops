---
phase: 01-local-safety-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["src/workspace.ts", "src/profile.ts", "src/policy.ts", "src/aiBoundary.ts", "tests/tdd/cn-job-ops/profile-policy.test.ts"]
autonomous: true
requirements: ["PROF-01", "PROF-02", "PROF-03", "PROF-04", "PROV-04"]
user_setup: []
must_haves:
  truths:
    - "Workspace init creates gitignored local data paths."
    - "Profile validation fails when minimum scoring fields are missing."
    - "PolicyGate blocks unsafe operations and unknown actions."
    - "AI prompt boundary redacts PII and non-allowlisted fields."
  artifacts: ["src/workspace.ts", "src/profile.ts", "src/policy.ts", "src/aiBoundary.ts"]
  key_links: ["profile-policy tests import and exercise all four modules"]
---

<objective>
Implement the local safety foundation using RED -> GREEN -> REFACTOR.
</objective>

<tasks>
- Write RED tests for init/profile/redaction/policy.
- Implement local workspace/profile/policy/AI boundary modules.
- Verify with `npm test`, `npm run typecheck`, and `npm run build`.
</tasks>

<verification>
- `npm test`
- `npm run typecheck`
- `npm run build`
</verification>
