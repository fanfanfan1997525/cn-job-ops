---
phase: 02-provider-discovery-mvp
plan: 01
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified: ["src/providers/registry.ts", "src/providers/fixture.ts", "src/providers/manual.ts", "src/providers/mcpJobs.ts", "src/audit.ts", "docs/PROVIDER-RISK.md"]
autonomous: true
requirements: ["PROV-01", "PROV-02", "PROV-03", "PROV-05", "QA-01"]
user_setup: []
must_haves:
  truths:
    - "Providers declare capabilities, trust status, rate limits, and failure types."
    - "Manual and fixture providers run locally."
    - "mcp-jobs is blocked by default and works only with injected opt-in runner."
    - "Unknown boss/liepin descriptors are not executable."
  artifacts: ["src/providers/registry.ts", "src/providers/mcpJobs.ts", "docs/PROVIDER-RISK.md"]
  key_links: ["provider-contract tests exercise registry, adapters, and audit"]
---

<objective>
Implement provider discovery and failure taxonomy without unsafe live automation.
</objective>

<tasks>
- Implement provider registry and descriptors.
- Implement manual, fixture, and injected `mcp-jobs` provider paths.
- Implement redacted audit logging.
- Document provider risk and trust levels.
</tasks>

<verification>
- `npm test`
- `npm audit --json`
</verification>
