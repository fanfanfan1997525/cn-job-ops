# MCP Placeholder

CN Job Ops v1 does not start an MCP server and does not depend on an MCP SDK.

The package contains only a dependency-free placeholder module so later work can add an MCP server without changing the core CLI contract. The v1 supported interface is the `cn-job-ops` CLI.
